--- a/net/minecraft/world/item/BlockItem.java
+++ b/net/minecraft/world/item/BlockItem.java
@@ -32,6 +_,12 @@
     @Deprecated
     private final Block block;
 
+    // Spout start - More data-driven - Minecraft registries - Types - Add codec for items
+    public com.mojang.serialization.MapCodec<? extends net.minecraft.world.item.BlockItem> codec() {
+        return spout.common.moredatadriven.minecraft.type.ItemCodecs.BLOCK_ITEM_CODEC;
+    }
+    // Spout end - More data-driven - Minecraft registries - Types - Add codec for items
+
     public BlockItem(final Block block, final Item.Properties properties) {
         super(properties);
         this.block = block;
@@ -223,6 +_,10 @@
 
     public void registerBlocks(final Map<Block, Item> map, final Item item) {
         map.put(this.getBlock(), item);
+        if (!this.getBlock().keyInBlockRegistry.equals(item.keyInItemRegistry)) {
+            System.out.println("Registered block -> item: " + this.getBlock().keyInBlockRegistry + " -> " + item.keyInItemRegistry);
+            spout.server.paper.impl.packetmapping.item.ItemMappingsImpl.get().addEventInitializer(event -> System.out.println("New itemstack: " + new ItemStack(this.getBlock())));
+        }
     }
 
     @Override
