--- a/src/main/java/org/bukkit/craftbukkit/CraftServer.java
+++ b/src/main/java/org/bukkit/craftbukkit/CraftServer.java
@@ -990,6 +_,7 @@
 
         org.spigotmc.SpigotConfig.init((File) this.console.options.valueOf("spigot-settings")); // Spigot
         this.console.paperConfigurations.reloadConfigs(this.console);
+        spout.server.paper.impl.configuration.SpoutConfigurations.get(null).reloadConfigs(this.console); // Spout - Configuration - Reload on Bukkit reload
         for (ServerLevel world : this.console.getAllLevels()) {
             // world.serverLevelData.setDifficulty(config.difficulty); // Paper - per level difficulty
             world.setSpawnSettings(world.isSpawningMonsters()); // Paper - per level difficulty (from MinecraftServer#setDifficulty(ServerLevel, Difficulty, boolean))
@@ -2724,6 +_,14 @@
         public YamlConfiguration getPaperConfig() {
             return CraftServer.this.console.paperConfigurations.createLegacyObject(CraftServer.this.console);
         }
+
+        // Spout start - Configuration - API - Implementation part
+        @Override
+        public YamlConfiguration getSpoutConfig()
+        {
+            return spout.server.paper.impl.configuration.SpoutConfigurations.get(null).createLegacyObject(CraftServer.this.console);
+        }
+        // Spout end - Configuration - API - Implementation part
 
         @Override
         public void restart() {
