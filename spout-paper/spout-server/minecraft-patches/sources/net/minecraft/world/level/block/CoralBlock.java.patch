--- a/net/minecraft/world/level/block/CoralBlock.java
+++ b/net/minecraft/world/level/block/CoralBlock.java
@@ -18,11 +_,10 @@
 import org.jspecify.annotations.Nullable;
 
 public class CoralBlock extends Block {
-    public static final MapCodec<Block> DEAD_CORAL_FIELD = BuiltInRegistries.BLOCK.byNameCodec().fieldOf("dead");
-    public static final MapCodec<CoralBlock> CODEC = RecordCodecBuilder.mapCodec(
-        i -> i.group(DEAD_CORAL_FIELD.forGetter(b -> b.deadBlock), propertiesCodec()).apply(i, CoralBlock::new)
-    );
-    private final Block deadBlock;
+    // Spout start - More data-driven - Data pack - Copy loaded blocks and items to their internal registries - Initialize some references late
+    public static final MapCodec<CoralBlock> CODEC = spout.common.moredatadriven.minecraft.type.BlockCodecs.coralCodec(CoralBlock::new);
+    public Block deadBlock;
+    // Spout end - More data-driven - Data pack - Copy loaded blocks and items to their internal registries - Initialize some references late
 
     public CoralBlock(final Block deadBlock, final BlockBehaviour.Properties properties) {
         super(properties);
